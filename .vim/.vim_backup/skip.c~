#include <stdio.h>

//void skip(char *msg)
void skip(char msg[])
{
	puts(msg + 6);
}

int main()
{
	//char *msg_from_amy = "Don't call me";
	char msg_from_amy[] = "Don't call me";
	printf("msg_from_amy的地址是%p\n", msg_from_amy);
	printf("msg_from_amy+1的地址是%p\n", msg_from_amy+1);
	//skip(msg_from_amy);
	
	int nums[] = {1, 2, 3};
	printf("nums的地址是%p\n", nums);
	printf("nums+1的地址是%p\n", nums+1);

	int doses[] = {1, 3, 2, 1000};
	printf("服用%i毫克的药\n", 3[doses]);

	return 0;
}
