/**
 *密码复杂度检测
 *@author fengshangshi
 */
module.exports = function(password) {
	var level = 0;
	/[0-9]+/.test(str) && ++level;
	/[a-z]+/.test(str) && ++level;
	/[A-Z]+/.test(str) && ++level;
	/[^A-z0-9]+/.test(str) && ++level;
	(passowrd.length > 5) && ++level;
	return level;
};
